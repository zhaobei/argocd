apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx{{ .Values.name }}
spec:
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: nginx{{ .Values.name }}
  replicas: 8
  template:
    metadata:
      labels:
        app: nginx{{ .Values.name }}
    spec:
      containers:
      - name: nginx{{ .Values.name }}
        image: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-service
spec:
  type: ClusterIP
  selector:
    app: nginx{{ .Values.name }}
  ports:
  - port: 8080
    targetPort: 80

